{% extends 'base.html.twig' %}

{% block body %}
    <div class="flex flex-col gap-3">
        {% for event in events %}
            <twig:EventCard :eventId="event.id" />
        {% endfor %}
    </div>
{% endblock %}
